---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { CALL_HREF } from "../../siteConfig";

const includes = [
  "A logo and wordmark: three directions explored, one finished, in every format you'll need",
  "Your colors, with simple rules for when to use what",
  "Your typefaces, with simple rules to keep things consistent",
  "A photo and illustration style, with real sample pieces",
  "Your brand kit: the file the AI tools read so everything comes out on-brand",
  "Templates for the things you make most: decks, social posts, headers, email signature",
  "A set of prompts tuned to your brand, so making new content is quick",
  "A short, friendly guide so anyone on your team can stay on-brand with confidence",
];
---

<BaseLayout title="Brand & Visual System Sprint · Nrvana Studio">
  <section class="section pt-24 pb-12">
    <div class="text-xs uppercase tracking-widest" style="color: var(--color-accent-alt);">For your team · 2–50 people</div>
    <h1 class="mt-4 font-display text-6xl font-semibold tracking-tight leading-[0.95] max-w-4xl">
      A whole brand your team will love. <em>Built in weeks, not quarters.</em>
    </h1>
    <p class="mt-6 max-w-2xl text-lg" style="color: var(--color-ink-soft);">
      Logo, colors, type, templates, and the simple kit that keeps everything consistent, built with
      AI, finished by hand, and easy for anyone on your team to use.
    </p>
  </section>

  <section class="section py-8">
    <div class="grid gap-4 rounded-2xl border p-8 sm:grid-cols-3"
         style="border-color: var(--color-line); background-color: var(--color-canvas-soft);">
      <div><div class="text-xs uppercase tracking-widest" style="color: var(--color-ink-mute);">Length</div><div class="mt-1 font-display text-2xl font-semibold">2–4 weeks</div></div>
      <div><div class="text-xs uppercase tracking-widest" style="color: var(--color-ink-mute);">Outcome</div><div class="mt-1 font-display text-2xl font-semibold">Full system + templates</div></div>
      <div><div class="text-xs uppercase tracking-widest" style="color: var(--color-ink-mute);">Fee</div><div class="mt-1 font-display text-2xl font-semibold" style="color: var(--color-accent-deep);">$5k–$15k</div></div>
    </div>
  </section>

  <section class="section py-12">
    <h2 class="font-display text-4xl font-semibold">What ships</h2>
    <ul class="mt-8 space-y-3" style="color: var(--color-ink-soft);">
      {includes.map((i) => <li>· {i}</li>)}
    </ul>
  </section>

  <section class="section py-20">
    <div class="rounded-2xl p-10 text-center" style="background-color: var(--color-ink); color: var(--color-canvas);">
      <h2 class="font-display text-4xl font-semibold">Let's build your brand.</h2>
      <p class="mt-3" style="color: var(--color-canvas-soft);">Start with a free chat. We'll map out exactly what you need.</p>
      <div class="mt-8">
        <a href={CALL_HREF} class="inline-flex items-center gap-2 rounded-md px-6 py-3 font-medium"
           style="background-color: var(--color-accent); color: var(--color-canvas);">Book a free 30-minute call</a>
      </div>
    </div>
  </section>
</BaseLayout>
